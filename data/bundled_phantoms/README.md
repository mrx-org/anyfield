# Bundled phantoms (main `22_niivue` repo)

Files here are loaded by the lab shell on startup (`loadBundledDefaultPhantom` in `niivue_app.js`).

- **`brain_default_1mm_gz/`** — `nifti_phantom_v1` JSON + maps (PD, dB0, B1+). Source of truth for edits may also live under `data/tool_phantomlib_flyio/data/` (nested repo); keep copies in sync if you maintain both.
